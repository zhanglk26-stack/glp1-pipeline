I have implemented the following changes:
1. Fixed sticky headers by using `sticky top-[64px]` on `th` elements.
2. Improved filter chip styling using Tailwind `peer` and `peer-checked` for better reliability.
3. Fixed a corruption in `js/app.js` and ensured the filter count updates correctly.
4. Cleaned up unnecessary `overflow` and `max-height` styles that were causing layout issues.

Please review the changes in index.html, predictor.html, js/app.js, and css/style.css.
